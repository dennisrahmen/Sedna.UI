@* SednaStateArt — writes the state illustrations' sprite into the page, once.
   See the XML doc comment on the partial class in SednaStateArt.razor.cs. *@
@namespace Sedna.UI

@((MarkupString)Sprite)
